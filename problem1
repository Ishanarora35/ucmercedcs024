#include "headers.h"
// I KNOW THIS ISN'T EFFICIENT CODE DONT HATE ON ME FR FR
void sum(int arr[]) {
    int x = 0;
    for (int i =0; i < 6; i++ ) {
        x += arr[i];
    }
    cout << x << endl;
}

void val(int arr[]) {
    int x = arr[0];
    for (int i = 0;i < 6; i++) {
    if (x < arr[i]) {
        x = arr[i];
    }
    }
    cout << x << endl;
}
void avg(int arr[]) {
        double x = 0;
    for (int i =0; i < 6; i++ ) {
        x += arr[i];
    }
    double z =  x/6;
    cout << z << endl;
}
int main () {
    int array[6];
    int a,b,c,d,e,f;
    cin >> a;
    cin >> b;
    cin >> c;
    cin >> d;
    cin >> e;
    cin >> f;
    array[0] = a;
    array[1] = b;
    array[2] = c;
    array[3] = d;
    array[4] = e;
    array[5] = f;


sum(array);
avg(array);
val(array);

}



